# React 学习网站

一个现代化的 React 学习平台，提供系统化的教程、实战案例和最佳实践指南。采用 React + Vite + Supabase 技术栈构建，支持响应式设计和移动端适配。

## ✨ 功能特性

- 📚 **系统化学习路径** - 从基础到进阶的完整学习体系
- 📝 **Markdown 支持** - 支持 Markdown 格式的文章编写和渲染
- 🎨 **代码高亮** - 内置代码语法高亮和复制功能
- 📱 **响应式设计** - 完美适配桌面端和移动端
- 🗂️ **目录树导航** - 清晰的层级结构和导航体验
- ⚡ **实时编辑** - 支持在线创建、编辑和发布文章
- 🔍 **全文搜索** - 快速搜索文章内容
- 🏷️ **标签系统** - 灵活的文章分类和标签管理
- 📊 **管理后台** - 完整的内容管理系统

## 🛠️ 技术栈

- **前端框架**: React 18
- **构建工具**: Vite
- **路由**: React Router DOM
- **样式**: Tailwind CSS
- **数据库**: Supabase (PostgreSQL)
- **Markdown**: react-markdown + remark-gfm
- **代码高亮**: highlight.js
- **图标**: Lucide React (SVG 图标)

## 📦 项目结构

```
react-learning-website/
├── public/                 # 静态资源
├── src/
│   ├── components/         # 可复用组件
│   │   ├── Header.jsx     # 顶部导航
│   │   ├── Sidebar.jsx    # 侧边栏目录
│   │   ├── Layout.jsx     # 布局组件
│   │   └── MarkdownRenderer.jsx # Markdown 渲染器
│   ├── pages/             # 页面组件
│   │   ├── HomePage.jsx   # 首页
│   │   ├── ArticlePage.jsx # 文章详情页
│   │   └── AdminPage.jsx  # 管理后台
│   ├── lib/
│   │   └── supabase.js    # Supabase 配置和 API
│   ├── App.jsx            # 应用根组件
│   ├── main.jsx          # 应用入口
│   └── index.css         # 全局样式
├── supabase/
│   └── schema.sql        # 数据库结构
├── package.json
├── vite.config.js
├── tailwind.config.js
└── README.md
```

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone <repository-url>
cd react-learning-website
```

### 2. 安装依赖

```bash
npm install
```

### 3. 配置环境变量

复制 `.env.example` 文件并重命名为 `.env`：

```bash
cp .env.example .env
```

编辑 `.env` 文件，填入你的 Supabase 配置：

```env
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**获取 Supabase 配置：**
1. 登录 [Supabase Dashboard](https://app.supabase.com)
2. 选择你的项目
3. 点击左侧菜单的 "Settings" → "API"
4. 复制 "Project URL" 和 "anon public" 密钥

### 4. 设置 Supabase 数据库

1. 在 [Supabase](https://supabase.com) 创建新项目
2. 在 SQL 编辑器中执行 `supabase/schema.sql` 文件中的 SQL 语句
3. 获取项目 URL 和 anon key 并配置到环境变量中

### 5. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:3000` 查看网站。

## 📊 数据库设计

### 表结构

#### directories (目录表)
- `id` - UUID 主键
- `name` - 目录名称
- `path` - 目录路径
- `parent_id` - 父目录 ID (自关联)
- `sort_order` - 排序顺序
- `description` - 目录描述
- `created_at` - 创建时间
- `updated_at` - 更新时间

#### articles (文章表)
- `id` - UUID 主键
- `title` - 文章标题
- `slug` - URL 别名 (唯一)
- `description` - 文章描述
- `content` - 文章内容 (Markdown)
- `directory_id` - 所属目录 ID
- `is_published` - 是否发布
- `sort_order` - 排序顺序
- `created_at` - 创建时间
- `updated_at` - 更新时间

#### tags (标签表)
- `id` - UUID 主键
- `name` - 标签名称 (唯一)
- `color` - 标签颜色
- `created_at` - 创建时间

#### article_tags (文章标签关联表)
- `article_id` - 文章 ID
- `tag_id` - 标签 ID

### 关系说明

- 目录支持无限层级嵌套 (通过 `parent_id` 自关联)
- 文章属于某个目录 (可选)
- 文章可以有多个标签 (多对多关系)
- 支持全文搜索 (基于 PostgreSQL 的 tsvector)

## 🔧 Supabase 配置

### 1. 创建项目

1. 访问 [Supabase Dashboard](https://app.supabase.com)
2. 点击 "New Project" 创建新项目
3. 选择组织和数据库密码
4. 等待项目初始化完成

### 2. 执行 SQL 脚本

1. 在项目仪表板中，点击左侧菜单的 "SQL Editor"
2. 点击 "New Query" 创建新查询
3. 复制 `supabase/schema.sql` 文件的内容并粘贴
4. 点击 "Run" 执行 SQL 脚本

### 3. 获取 API 密钥

1. 在项目仪表板中，点击左侧菜单的 "Settings"
2. 选择 "API" 选项卡
3. 复制 "Project URL" 和 "anon public" 密钥
4. 将这些值配置到 `.env` 文件中

### 4. 配置 RLS (行级安全)

如果需要用户认证和权限控制，可以启用 RLS：

```sql
-- 启用 RLS
ALTER TABLE articles ENABLE ROW LEVEL SECURITY;
ALTER TABLE directories ENABLE ROW LEVEL SECURITY;

-- 创建策略 (示例：允许所有人读取已发布的文章)
CREATE POLICY "Allow public read access to published articles" 
ON articles FOR SELECT 
USING (is_published = true);

-- 允许所有人读取目录
CREATE POLICY "Allow public read access to directories" 
ON directories FOR SELECT 
USING (true);
```

## 📱 移动端适配

项目采用响应式设计，主要适配特性：

- **断点设计**: 使用 Tailwind CSS 的响应式断点
- **移动端导航**: 侧边栏在移动端转为抽屉式导航
- **触摸优化**: 按钮和链接针对触摸操作优化
- **字体缩放**: 支持系统字体大小设置
- **横屏适配**: 支持横屏和竖屏切换

### 主要断点

- `sm`: 640px 及以上
- `md`: 768px 及以上  
- `lg`: 1024px 及以上
- `xl`: 1280px 及以上

## 🎨 UI 设计

### 设计原则

- **简洁明了**: 参考 React 官网的简洁设计风格
- **易于阅读**: 优化的排版和间距
- **一致性**: 统一的颜色、字体和组件样式
- **可访问性**: 支持键盘导航和屏幕阅读器

### 颜色方案

```css
/* 主色调 */
--primary-50: #eff6ff;
--primary-500: #3b82f6;
--primary-600: #2563eb;
--primary-700: #1d4ed8;

/* 灰色调 */
--gray-50: #f9fafb;
--gray-100: #f3f4f6;
--gray-500: #6b7280;
--gray-900: #111827;
```

### 字体

- **无衬线字体**: Inter (主要文本)
- **等宽字体**: Fira Code (代码显示)

## 📝 内容管理

### 创建文章

1. 访问 `/admin` 管理后台
2. 点击 "新建文章" 按钮
3. 填写文章信息：
   - 标题 (必填)
   - URL 别名 (自动生成，可修改)
   - 所属目录 (可选)
   - 文章描述 (可选)
   - 文章内容 (Markdown 格式，必填)
   - 发布状态

### Markdown 支持

支持标准 Markdown 语法和扩展功能：

- **基础语法**: 标题、段落、列表、链接、图片
- **代码块**: 语法高亮和复制功能
- **表格**: GitHub 风格的表格
- **引用**: 块引用样式
- **任务列表**: `- [ ]` 和 `- [x]`

### 代码示例

```jsx
function Welcome({ name }) {
  return <h1>Hello, {name}!</h1>;
}
```

## 🔍 搜索功能

项目支持全文搜索，搜索范围包括：

- 文章标题
- 文章描述  
- 文章内容

搜索使用 PostgreSQL 的全文搜索功能，支持中文分词。

## 🚀 部署到 GitHub Pages

### 自动部署 (推荐)

项目已配置 GitHub Actions 自动部署：

1. **配置仓库**
   - 将代码推送到 GitHub 仓库
   - 在仓库设置中启用 GitHub Pages
   - 选择 "GitHub Actions" 作为部署源

2. **配置环境变量**
   在仓库的 Settings → Secrets and variables → Actions 中添加：
   ```
   VITE_SUPABASE_URL=your_supabase_project_url
   VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
   ```

3. **自动部署**
   - 推送到 main 分支会自动触发部署
   - 部署完成后访问：`https://your-username.github.io/react-learning-website/`

### 手动部署

```bash
# 安装依赖
npm install

# 构建并部署
npm run deploy
```

### 配置说明

- `vite.config.js` 中的 `base` 配置为仓库名
- GitHub Actions 工作流在 `.github/workflows/deploy.yml`
- 部署脚本使用 `gh-pages` 包

## 🤝 贡献指南

欢迎贡献代码和内容！

### 开发流程

1. Fork 项目
2. 创建功能分支：`git checkout -b feature/new-feature`
3. 提交更改：`git commit -am 'Add new feature'`
4. 推送分支：`git push origin feature/new-feature`
5. 创建 Pull Request

### 代码规范

- 使用 ESLint 进行代码检查
- 遵循 React 最佳实践
- 组件使用函数式组件和 Hooks
- 样式使用 Tailwind CSS 类名

## 📄 许可证

MIT License

## 🆘 常见问题

### Q: 如何添加新的学习目录？

A: 在管理后台中，你可以通过数据库直接插入新的目录记录，或者扩展管理界面添加目录管理功能。

### Q: 如何自定义主题颜色？

A: 修改 `tailwind.config.js` 文件中的颜色配置，然后重新构建项目。

### Q: 如何添加用户认证？

A: 可以使用 Supabase Auth 添加用户认证功能，并配置相应的 RLS 策略。

### Q: 如何优化 SEO？

A: 可以添加 React Helmet 来管理页面的 meta 标签，并实现服务端渲染 (SSR)。

## 📞 联系方式

如有问题或建议，请通过以下方式联系：

- 创建 GitHub Issue
- 发送邮件到 [your-email@example.com]

---

**Happy Learning! 🎉**